:- compiler_options([xpp_on]).
#include "smp.P"
#include "smp_data.P"
tabled_predicate(test1,0).

tr_clause(
	test1,
		seq(achieve(played2(michael,zohan)),
		seq(avoid(played1(michael,zohan)),
		seq(played2(michael,zohan),
		not(played1(michael,zohan)))))).

test_description('Test of Analysis of Stat Modifying System').
test_query(test1).

